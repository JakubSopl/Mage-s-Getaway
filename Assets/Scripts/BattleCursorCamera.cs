using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleCursorCamera : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        Cursor.visible = true;
    }
}
